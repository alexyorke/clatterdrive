# fake_hdd_fuse

`fake_hdd_fuse` is a bit of a novelty project with a fairly serious simulation core: it makes a normal directory feel and sound more like a mechanical hard drive.

The premise is intentionally playful. The implementation is still trying to be surprisingly plausible.

- It exposes a directory over WebDAV and injects HDD-like latency into file operations.
- It models seek, rotational delay, transfer rate, write-back behavior, metadata traffic, and fragmentation effects.
- It synthesizes the drive noise procedurally from runtime telemetry.
- It does **not** use prerecorded HDD sound effects.

## What This Is

This project is meant to be fun first:

- If the idea of turning a fast machine into a pretend hard drive makes you laugh, that is the correct reaction.
- If you want a toy project that still tries to behave plausibly under sequential reads, random reads, write-back flushes, and fragmented files, that is also the correct reaction.

So the right framing is:

- **Yes, it is a bit of a novelty project.**
- **Yes, it is also trying to be surprisingly realistic within that joke.**

## What Makes It Fairly Realistic

The simulator currently includes:

- A virtual filesystem allocator that emits both file-data and metadata I/O.
- A zoned HDD latency model with seek, rotation, transfer time, cache state, and idle power-state behavior.
- A scheduler that does request merging and LOOK/deadline-like dispatch instead of naive FIFO.
- Delayed write-back flushing rather than instant fake completion.
- A procedural audio engine driven by the same simulated events.

The sound path is fully synthetic:

- spindle hum is generated from harmonics and noise
- actuator noise is modal/resonant synthesis driven by seek and flush events
- calibration and park events are synthesized from telemetry

There are no bundled recorded drive noises hiding underneath.

## Sample Audio

GitHub strips inline audio players from normal repository READMEs, so the samples are linked directly as WAV files. Click them to open or download them from the repo view.

| Scenario | What You Should Hear | File |
| --- | --- | --- |
| Spin-up, idle, park | the full modeled power-on sequence: electronics init, spindle unlock, spin-up, self-test, head load, servo lock, a short idle period, then park | [spinup-idle-park.wav](samples/spinup-idle-park.wav) |
| Sequential read stream | one larger initial seek followed by a smoother streaming/purring read pattern | [sequential-read-stream.wav](samples/sequential-read-stream.wav) |
| Random seek + journal + flush | choppier actuator activity with sharper journal/flush-style events | [random-seek-journal-flush.wav](samples/random-seek-journal-flush.wav) |

These files are generated by the repo itself via [generate_audio_samples.py](generate_audio_samples.py).

## How It Works

The data path is:

`WebDAV -> virtual FS -> metadata/data extent planning -> scheduler -> HDD latency model -> procedural audio telemetry`

Important files:

- [main.py](main.py): starts the WebDAV server and audio engine
- [vfs_provider.py](vfs_provider.py): intercepts file reads and writes
- [fs_simulator.py](fs_simulator.py): fake filesystem allocation plus metadata I/O
- [os_scheduler.py](os_scheduler.py): request merging and dispatch policy
- [hdd_model.py](hdd_model.py): geometry, caches, timing, power states, write-back flushing
- [audio_engine.py](audio_engine.py): procedural HDD sound synthesis

## Running It

Install the Python dependencies you need for your environment, then start the server:

```powershell
python main.py
```

By default it serves `backing_storage/` over WebDAV on:

- `http://127.0.0.1:8080`

If you already have something bound to port `8080`, set `FAKE_HDD_PORT` instead of editing code:

```powershell
$env:FAKE_HDD_PORT = "8090"
python main.py
```

## Regenerating The Sample Audio

To rebuild the sample WAV files:

```powershell
python generate_audio_samples.py
```

That writes deterministic sample outputs into `samples/`. The spin-up sample is generated from the same staged startup model the runtime uses, rather than a separate hand-authored timeline.

## Profiling And Tests

Core profiling:

```powershell
python profile_core.py
python profile_fragmentation.py
```

Unit tests:

```powershell
python -m pytest -q
```

## Tuning Notes

If you want it to feel more or less aggressive, the main knobs live in [hdd_model.py](hdd_model.py):

- `avg_seek_ms`
- `track_to_track_ms`
- `head_switch_ms`
- `read_ahead_kb`
- `write_cache_mb`
- `dirty_expire_ms`
- `standby_after_s`
- `spinup_ms`

The audio character mostly lives in [audio_engine.py](audio_engine.py).

## Limitations

This is still a simulation, not a real block device:

- it is not implementing a full kernel page cache or full filesystem semantics
- folder-level WebDAV metadata paths are less detailed than file-stream paths
- it is intentionally compact and hackable, not a complete storage emulator

That said, it is much closer to "modeled fake HDD" than "sleep calls plus MP3s".
